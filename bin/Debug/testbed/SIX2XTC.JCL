.JCL to convert a TRSDOS 6.2.x system diskette to TRSDOS 6.2.x+. Insert the TRSDOS 6.2.x+ system diskette in drive #S#. Insert the TRSDOS 6.2.x system diskette in drive #D#BACKUP SYS0/SYS:#S# :#D# (S,I,OLD)BACKUP SYS2/SYS:#S# :#D# (S,I,OLD)BACKUP SYS3/SYS:#S# :#D# (S,I,OLD)BACKUP SYS6/SYS:#S# :#D# (S,I,OLD)BACKUP SYS7/SYS:#S# :#D# (S,I,OLD)BACKUP BACKUP/CMD:#S# :#D# (S,I)BACKUP FORMAT/CMD:#S# :#D# (S,I)BACKUP MEMDISK/DCT:#S# :#D# (S,I)BACKUP DATECONV/CMD:#S# :#D#DATECONV:#S# (D=#D#,C). Remove the diskette from drive #D# and label it with a "+"//EXIT